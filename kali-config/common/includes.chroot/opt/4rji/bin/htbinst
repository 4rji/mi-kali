#!/usr/bin/env bash

echo """


target1

iniciarhtb

iniciar2


expo mas signo de dolarIP para enumerar.



desde la configuracion de singlefile de firefox, se modifica para que guarde la pagina web en clipboard desde la configuracion 
se puede guardar la pagina descargar y moverla de downloads a content o tambien:

de la extension dentro de firefox, en Destination, copy to clipboard
y ya despues en la terminal dentro de donde se quiera guarda se hace un
pega > archivo.index 
y listo, pega es un alias de pegar el clipboard. 


Debo definir esta ruta para que funcione 
htcon=ruta de content para guardar las contrasenas


usua = agrega usuarios al archivo users en el folder content
contra = agrega contrasenas al archivo passwords en content

pas = copia lo que tenga en el clipboard al archivo nombre_maquina.md

htf= ruta al archivo de donde se guardan los walk.md
usualment es en folder de la maquina y el nombre de la maquina.md

asd= copia la ip de target1 

datos = para crear la base de datos de la pagina web busqueda en arch, este paso es al final.

hacer un alias de 

qwe = alias 'clipc && pas'
para copiar el ultimo comando al archivo maquina.md"""


echo ""
